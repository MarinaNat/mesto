/*class Renderable {
    constructor(cardTemplateSelector) {
        this.element = document.querySelector(cardTemplateSelector).content.children[0].cloneNode(true);
    }

    render(where) {
        where.appendChild(this._element);
    }
}*/